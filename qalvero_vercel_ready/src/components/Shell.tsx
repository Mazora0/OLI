import { Link,NavLink } from 'react-router-dom';import { Languages, Moon, Sun, User, Menu } from 'lucide-react';import { useState } from 'react';import Logo from './Logo';import { useApp } from '../context/AppContext';import { languages,tr } from '../lib/i18n';
export default function Shell({children}:{children:React.ReactNode}){const {lang,setLang,theme,toggleTheme,user,logout}=useApp();const [open,setOpen]=useState(false);const links=[['/','AI Chat'],['/products','Products'],['/pricing','Pricing'],['/dashboard','Dashboard'],['/settings','Settings']];return <div className="min-h-screen bg-[#080A12] text-slate-50 dark:bg-[#080A12] light:bg-slate-50 light:text-slate-950"><header className="sticky top-0 z-40 border-b border-white/10 bg-[#080A12]/85 backdrop-blur light:bg-white/85"><div className="mx-auto flex max-w-7xl items-center justify-between px-3 py-3 sm:px-6"><Link to="/"><Logo/></Link><nav className="hidden items-center gap-1 md:flex">{links.map(([to,label])=><NavLink key={to} to={to} className={({isActive})=>`rounded-xl px-3 py-2 text-sm ${isActive?'bg-white/10 text-white light:bg-slate-900 light:text-white':'text-slate-400 hover:bg-white/5 light:text-slate-600'}`}>{label}</NavLink>)}</nav><div className="flex items-center gap-2"><select aria-label="Language" value={lang} onChange={e=>setLang(e.target.value as any)} className="hidden rounded-xl border border-white/10 bg-white/5 px-2 py-2 text-xs outline-none light:bg-white sm:block">{languages.map(([v,l])=><option className="bg-slate-950" key={v} value={v}>{l}</option>)}</select><button onClick={toggleTheme} className="rounded-xl border border-white/10 p-2 hover:bg-white/10" aria-label="Toggle theme">{theme==='dark'?<Sun size={18}/>:<Moon size={18}/>}</button>{user?<button onClick={logout} className="hidden rounded-xl bg-white px-3 py-2 text-sm font-semibold text-slate-950 md:block">{tr(lang,'logout')}</button>:<Link to="/login" className="hidden rounded-xl bg-white px-3 py-2 text-sm font-semibold text-slate-950 md:block">{tr(lang,'login')}</Link>}<button onClick={()=>setOpen(!open)} className="rounded-xl border border-white/10 p-2 md:hidden"><Menu size={18}/></button></div></div>{open&&<div className="border-t border-white/10 p-3 md:hidden">{links.map(([to,label])=><Link onClick={()=>setOpen(false)} className="block rounded-xl px-3 py-3 text-slate-300 hover:bg-white/10" to={to} key={to}>{label}</Link>)}<div className="mt-2 flex gap-2"><select value={lang} onChange={e=>setLang(e.target.value as any)} className="w-full rounded-xl bg-white/10 p-3">{languages.map(([v,l])=><option className="bg-slate-950" value={v} key={v}>{l}</option>)}</select></div></div>}</header><main>{children}</main><footer className="border-t border-white/10 px-4 py-8 text-center text-sm text-slate-500">Qalvero LLC • Software, AI tools, mobile applications, SaaS • support@qalvero.com • <Link to="/legal" className="text-cyan-300">Legal</Link></footer></div>}
